
  # Build Scheduling and EMR Widgets

  This is a code bundle for Build Scheduling and EMR Widgets. The original project is available at https://www.figma.com/design/BPrnTI1HEgeEcMIEnLglnb/Build-Scheduling-and-EMR-Widgets.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  